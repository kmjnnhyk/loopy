node_modules/
dist/
dist-examples/
*.tsbuildinfo
.remember/
test-results/
playwright-report/

# Internal design / planning / brainstorming artifacts — kept on disk locally,
# not published to the public repo. Root-anchored (/docs/) so it does NOT match
# the docs site's own content at website/src/content/docs/.
/docs/
/loopy-vs-langchain.html
