# __NAME__

A loopy starter. Run it:

```bash
bun install
bun run dev   # DevTools UI at http://localhost:5173
```

`index.ts` defines a one-node `hello` workflow — no model, no API key.
See https://loopy.js.org/getting-started/ for the full guide.
